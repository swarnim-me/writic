<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Writic</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous">
    <link rel="stylesheet" href="/styles.css">
</head>

<body>
    <nav class="navbar bg-dark navbar-dark navbar-expand-lg mb-4">
        <div class="container">
            <a href="/" class="navbar-brand">Writic</a>
            <button class="navbar-toggler" data-target="#menu" data-toggle="collapse">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="menu">
                <% if(currentUser){ %>
                    <ul class="navbar-nav mr-auto">
                        <li class="nav-item"><a href="#" class="nav-link">Signed in as <%=currentUser.firstname%></a></li>
                        <li class="nav-item"><a href="/<%=currentUser.username%>/saved" class="nav-link">Saved Posts</a></li>
                        <li class="nav-item"><a href="/<%=currentUser.username%>/settings" class="nav-link">Settings</a></li>
                        <li class="nav-item"><a href="/auth/logout" class="nav-link">Logout</a></li>
                    </ul>
                    <form class="form-inline my-2 my-lg-0" method="GET" action="/user/search">
                        <input class="form-control mr-sm-2" type="search" placeholder="Enter Username" aria-label="Username"  name="searchUser">
                        <button class="btn btn-outline-light my-2 my-sm-0" type="submit">Search</button>
                      </form>
                    
                <%}%>
                <% if(!currentUser){ %>
                    <ul class="navbar-nav ml-auto">
                        <li class="nav-item"><a href="/auth/login" class="nav-link">Login</a></li>
                        <li class="nav-item"><a href="/auth/register" class="nav-link">Register</a></li>
                    </ul>
                <%}%>
            </div>
        </div>
    </nav>
    <div class="container">

        <% if (error.length>0){ %>
            <div class="alert alert-danger alert-dismissible fade show mb-4" role="alert">
                <%= error %>
                <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                  <span aria-hidden="true">&times;</span>
                </button>
              </div>
        <% } %>
        <% if (success.length>0){ %>
            <div class="alert alert-success alert-dismissible fade show mb-4" role="alert">
                <%= success %>
                <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                  <span aria-hidden="true">&times;</span>
                </button>
              </div>
        <% } %>